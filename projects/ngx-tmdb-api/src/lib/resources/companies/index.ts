import { getDetails } from './details.request';
import { getImages } from './images.request';
import { getAlternativeNames } from './alternative-names.request';

export const Companies = {
  getImages,
  getDetails,
  getAlternativeNames
};
